package WhiteLightning.Oel.game;

//Class for having under hand some commented code which probably would be thrown out
public class Junk {

	/*
	  
	  
// c = batch.getColor();
// batch.setColor(c.r, c.g, c.b, 0.5f);


Menu line:

		if (flag1) {
			z = z + 3;
			System.out.println("z: " + z);
			if (z >= 150) {
				z = 1;
				flag1 = false;
			}

			batch.draw(line, x - 12, y - 20 - selectedItem * deltaY, z, 6);
		}
	 
	 */
	
	
	
}
