package WhiteLightning.Oel.game;

/*
 * Like good practice says we shouldn't have such code in project...
 * But in overall its better to have junk here and delete it later:)
 */
public class Junk {

	/*
	  
	  
// c = batch.getColor();
// batch.setColor(c.r, c.g, c.b, 0.5f);


Menu line:

		if (flag1) {
			z = z + 3;
			System.out.println("z: " + z);
			if (z >= 150) {
				z = 1;
				flag1 = false;
			}

			batch.draw(line, x - 12, y - 20 - selectedItem * deltaY, z, 6);
		}
	 
	 */
	
	
	
	
	
/*		
	private void drawSetup() {
		drawHUD();
		batch.begin();
		font.setScale(.9f, .9f);		
		font.draw(batch, "Input: " + s.sb,500,200);	
	//	Skin skin = new Skin();	
	//	String value=s.text.getText();
		s.text.setBounds(200, 200, 100, 40);
		s.text.draw(batch, 1);
		s.text.getOnscreenKeyboard();
	//	CheckBox box=new CheckBox("done",skin);
	//	boolean checked=box.isChecked();
	//	String[] items={"cool","mega","awesome"};
	//	SelectBox selectbox=new SelectBox(skin);
	//	selectbox.setItems(items);
	//	String selection=selectbox.getSelected().toString();	
		batch.end();		
	//	TextInput listener = new TextInput();
	//	Gdx.input.getTextInput(listener, "Ala", "ma", "kota");		
	}
	*/
	
	
	
	
	
	
	
	
	
}
